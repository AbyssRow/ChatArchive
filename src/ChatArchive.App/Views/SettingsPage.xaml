<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ChatArchive.App.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChatArchive.App.Views"
    NavigationCacheMode="Required">
    <ScrollViewer x:Name="SettingsPane" Padding="32,24">
        <StackPanel MaxWidth="800" Spacing="24" HorizontalAlignment="Left">
            <StackPanel Spacing="4">
                <TextBlock Text="设置" FontSize="26" FontWeight="SemiBold" />
                <TextBlock Text="管理数据存储位置与系统配置" FontSize="13" Opacity="0.6" />
            </StackPanel>

            <!-- 存储设置卡片 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="20">
                <StackPanel Spacing="16">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <FontIcon Glyph="&#xE8B7;" FontSize="20" Foreground="{ThemeResource SystemAccentColor}" />
                        <TextBlock Text="数据存储目录" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" />
                    </StackPanel>

                    <TextBlock Text="当前所有聊天记录数据库、媒体文件与头像均保存在以下目录：" FontSize="13" Opacity="0.75" />

                    <TextBox x:Name="SettingsDataPathText" IsReadOnly="True" FontSize="13" />

                    <!-- 存储使用量信息 -->
                    <Grid ColumnSpacing="16" RowSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="6" Padding="12,8">
                            <StackPanel Spacing="2">
                                <TextBlock Text="总空间占用" FontSize="11" Opacity="0.6" />
                                <TextBlock x:Name="SettingsTotalSizeText" Text="--" FontSize="15" FontWeight="SemiBold" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="6" Padding="12,8">
                            <StackPanel Spacing="2">
                                <TextBlock Text="数据库大小" FontSize="11" Opacity="0.6" />
                                <TextBlock x:Name="SettingsDbSizeText" Text="--" FontSize="15" FontWeight="SemiBold" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="6" Padding="12,8">
                            <StackPanel Spacing="2">
                                <TextBlock Text="媒体文件" FontSize="11" Opacity="0.6" />
                                <TextBlock x:Name="SettingsMediaSizeText" Text="--" FontSize="15" FontWeight="SemiBold" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="3" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="6" Padding="12,8">
                            <StackPanel Spacing="2">
                                <TextBlock Text="头像缓存" FontSize="11" Opacity="0.6" />
                                <TextBlock x:Name="SettingsAvatarSizeText" Text="--" FontSize="15" FontWeight="SemiBold" />
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- 操作按钮栏 -->
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button x:Name="SettingsChangeDirButton" Content="更改存储目录…" Style="{ThemeResource AccentButtonStyle}" Click="OnSettingsChangeDirClick" />
                        <Button x:Name="SettingsOpenDirButton" Content="在资源管理器中打开" Click="OnSettingsOpenDirClick" />
                        <Button x:Name="SettingsResetDirButton" Content="恢复默认目录" Click="OnSettingsResetDirClick" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- 关于与系统信息卡片 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="20">
                <StackPanel Spacing="12">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <FontIcon Glyph="&#xE946;" FontSize="20" Foreground="{ThemeResource SystemAccentColor}" />
                        <TextBlock Text="关于 ChatArchive" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" />
                    </StackPanel>
                    <TextBlock Text="基于 WinUI 3 + .NET 10 的现代化本地聊天归档与多维检索系统。" FontSize="13" Opacity="0.75" />
                    <TextBlock Text="所有数据与附件均 100% 存放于本地，无需联网，保护个人隐私。" FontSize="12" Opacity="0.6" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
